import maya.cmds as cmds # pyright: ignore[reportMissingImports] 
import autoRigger.naming as naming
import autoRigger.modules.limbModule as limbModule

#RIBBON MAKER

class ribbonMaker:
    def __init__(self, joints): 
        self.suffix = naming.suffix
        self.prefix = naming.prefix

        self.joints = joints


    def creatingSurface(self): 
        '''
        responsible for creating the nurbs plane that will deform the joints.

        returns:
        
        '''

        cmds.nurbsPlane( )